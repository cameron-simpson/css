DBOverlay: SQLTags subclas which can scan a db via an sqla ORM and update keys, and specific keys update the db via the ORM
derive SQL conditions from test value - ~:in ~/:regexp match
can SQLTagProxies be dropped in favour of the .subdomain() method?

