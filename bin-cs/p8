#!/bin/sh
#
# Convenient invocation of autopep8.
#   - Cameron Simpson <cs@zip.com.au> 20mar2015
#

set -uex

mode=--diff
[ $# -gt 0 ] && [ "x$1" = x-y ] && mode=--in-place

venv exec autopep8 "$mode" --indent-size 2 ${1+"$@"}
