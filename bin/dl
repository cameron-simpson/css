#!/bin/sh -u
#
# Log an action via buglog.
#	- Cameron Simpson <cs@zip.com.au> 06sep2004
#

: ${BUGSYSTEMS:=dlog}

case " $BUGSYSTEMS " in
  *' dlog '*) ;;
  *)  BUGSYSTEMS="dlog $BUGSYSTEMS" ;;
esac
export BUGSYSTEMS

exec bgproc -p /dev/null -l buglog buglog -n -- "$*"
