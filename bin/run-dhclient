#!/bin/sh
#
# Run a dhclient on the specified interface
#   - Cameron Simpson <cs@zip.com.au> 09apr2017
#

[ $# = 0 ] || { echo "usage: $0 ifname" >&2; exit 2; }
ifname=$1
shift
set -x
exec svcd -n dhclient -- dhclient -d "$ifname"
