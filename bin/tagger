#!/usr/bin/env python3
#
# Run the cs.app.tagger module command line mode.
# - Cameron Simpson <cs@cskk.id.au>
#

import os
import sys
sys.path[:] = [ path for path in sys.path if path ]
from cs.app.tagger import main
sys.exit(main(sys.argv))
