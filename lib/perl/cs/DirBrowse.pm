#!/usr/bin/perl
#

use strict qw(vars);

##BEGIN { use cs::DEBUG; cs::DEBUG::using(__FILE__); }

package DirBrowse;

@cs::DirBrowse::ISA=qw();

sub new
	{ my($class,$type)=(shift,shift);

	  if ($type eq 
	}

1;
