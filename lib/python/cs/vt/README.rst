Content address data store and filesystem somewhat akin to Venti.
=================================================================

Installation
------------

Requirements: python 3 and the llfuse module.

* OS/X: install osxfuse, for example from MacPorts.
* "pip install llfuse" for your Python 3.
