#!/bin/sh
#
# Process the mail item on stdin as spam.
#	- Cameron Simpson <cs@zip.com.au>
#

: ${LOGDIR:=$HOME/var/log}
: ${ALERTLOG:=$LOGDIR/alert}

exec 3<&0 >>"$ALERTLOG" 2>&1
bgproc -p /dev/null -i - -l alert set-x bogofilter -Ns <&3 3<&- &
exit 0
