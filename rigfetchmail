#!/bin/sh
#
# Dispatch a daemon mode fetchmail.
#	- Cameron Simpson <cs@zip.com.au> 14dec2003
#
needhost amadeus@home || exit 1
set -x
exec fetchmail --daemon 1800 -f $HOME/var/fetchmail/fetchmailrc -a \
	mail.carlnfd1.nsw.optushome.com.au \
	mail.zipworld.com.au
