#!/bin/sh

cd $HOME/rc/x11 || exit $?

case "$X11_DEPTH,$X11_CLASS" in
    1,*)			type=Mono ;;
    *,StaticGray|*,GrayScale)	type=Grey ;;
    *)				type=Colour ;;
esac
# set-x \
myke -u \
     -f Mykefile.${X11_X}x${X11_Y} \
     -f Mykefile.$type \
     -f Mykefile \
     "type=$type" _dflts
exec xrdb -nocpp -load $HOME/.Xdefaults

dpp ${1+"$@"} $X11/xinfo.dpp $X11/defaults \
      | xrdb -load -nocpp
